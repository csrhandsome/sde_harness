# Global serving defaults. Model-specific settings live in models/<id>/model.yaml.
active: hy-embodied-vlm-1.0
models_dir: models
cache_dir: cache

defaults:
  host: "0.0.0.0"
  port: 8080
  hf_endpoint: "https://hf-mirror.com"
