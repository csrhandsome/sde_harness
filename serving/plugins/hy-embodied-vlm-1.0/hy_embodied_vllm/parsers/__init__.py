"""Namespace file so `hy_embodied_vllm.parsers` is a valid submodule.

The `.parsers/` subdirectory ships the tool_parser + reasoning_parser
implementations that get registered into vLLM's parser managers when
the plugin's top-level `register()` runs.
"""
