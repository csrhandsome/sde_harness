# Hy-Embodied vLLM plugin

Vendored vLLM model and parser registrations for
`tencent/Hy-Embodied-VLM-1.0`. The serving launcher is intentionally kept in
`vla_serving.runtimes.vllm`; this package contains plugin code only.

See `UPSTREAM.txt` for provenance.
